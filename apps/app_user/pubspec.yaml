name: app_user
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 26.06.12-dev-staging+26061201
resolution: workspace

environment:
  sdk: ^3.10.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  cupertino_icons: ^1.0.8
  file_picker: ^12.1.3  # iOS: native file picker
  flutter_displaymode: ^0.7.0  # Android: request 120Hz/ProMotion high refresh rate
  firebase_core: ^4.3.0
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_native_splash: ^2.4.3
  flutter_quill: ^11.5.0
  flutter_riverpod: ^3.1.0
  flutter_secure_storage: ^11.0.0  # iOS: Keychain
  freezed_annotation: ^3.1.0
  go_router: ^18.0.0
  google_sign_in: 7.2.0  # iOS: GoogleService-Info.plist required
  http: ^1.6.0
  intl: ^0.20.2
  kakao_map_plugin: ^0.4.0
  minglit_kit:
    path: ../../shared/packages/minglit_kit
  # P4: demo flavor entry (lib/main_demo.dart) imports demoOverrides().
  # Tree-shaking removes this from prod APK because lib/main.dart never
  # transitively imports it. Verified by Success Metric "Prod 의존성 격리".
  minglit_demo:
    path: ../../shared/packages/minglit_demo
  path_provider: ^2.1.5
  qr_flutter: ^4.1.0
  riverpod: ^3.1.0
  riverpod_annotation: ^4.0.0
  package_info_plus: ^10.2.1  # iOS: CFBundleVersion (Info.plist)
  screen_brightness: ^2.1.7  # iOS: UIScreen brightness API
  sentry_flutter: ^10.0.0-alpha.4  # iOS: native crash reporting
  share_plus: ^13.3.0  # iOS: share sheet
  shared_preferences: ^2.5.4
  supabase_flutter: ^2.12.0
  url_launcher: ^6.3.2  # iOS: LSApplicationQueriesSchemes (Info.plist)

dev_dependencies:
  allure_report: ^1.0.0
  build_runner: ^2.10.4
  custom_lint: 0.8.1
  flutter_lints: ^6.0.0
  # `minglit_demo` moved to regular `dependencies` for main_demo.dart entry.
  # Tests reach it transitively via the regular dep.
  flutter_test:
    sdk: flutter
  freezed: ^3.2.3
  go_router_builder: ^4.1.3
  integration_test:
    sdk: flutter
  minglit_lints:
    path: ../../shared/packages/minglit_lints
  mocktail: ^1.0.4
  patrol: ^4.5.0
  patrol_finders: ^3.2.0
  plugin_platform_interface: ^2.1.8
  postgres: ^3.5.9
  riverpod_generator: ^4.0.0+1
  riverpod_lint: 3.1.0
  test: any
  test_reporter: ^1.3.0
  url_launcher_platform_interface: ^2.3.2
  uuid: ^4.5.2
  very_good_analysis: ^10.0.0

# For information on the generic Dart part of this file, see the
flutter:
  config:
    enable-swift-package-manager: false
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/

patrol:
  app_name: Minglit Dev
  android:
    package_name: com.minglit.app_user.dev
  ios:
    bundle_id: com.minglit.appUser.dev
