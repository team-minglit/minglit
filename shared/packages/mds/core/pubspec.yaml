name: mds
description: "Minglit Design System — pure UI components, theme, design tokens"
version: 26.04.1865-dev
resolution: workspace
publish_to: 'none'
environment:
  sdk: ^3.10.4

dependencies:
  file_picker: ^12.1.3
  flutter:
    sdk: flutter
  flutter_quill: ^11.5.0
  flutter_riverpod: ^3.1.0
  flutter_svg: ^2.2.2
  image_picker: ^1.2.1
  mds_tokens:
    path: ../tokens
  shimmer: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
